# Changelog — Meow Gateway v0.9.0 → v0.9.1

## 🐛 Bug Fixes
- Providers UI: Fixed nested modal body rendering bug in `EditProviderModal` where compound components wrapped inside React Fragments triggered fallback modal body container wrapping.
- BaseModal: Enhanced compound component detection to recursively inspect React Fragments when checking for ModalHeader, ModalBody, and ModalFooter.

## 🧹 Internal & Docs
- Bumped workspace packages and gateway default version to v0.9.1.
